UPDATE work_team_member_credentials
SET is_active = 0, updated_at = NOW()
WHERE wtm_cred_id = {{
  tbl_wtm_creds_i.triggeredRow?.wtm_cred_id
  || tbl_wtm_creds_i.selectedRow?.wtm_cred_id
  || tbl_wtm_creds.triggeredRow?.wtm_cred_id
  || tbl_wtm_creds.selectedRow?.wtm_cred_id
}};
