INSERT INTO work_team_member_credentials
(work_team_member_id, system_code, username, alias_email, password_text, cat_profile, is_active, origin_uncred_id)
SELECT
  {{ appsmith.store.wtmRow?.work_team_member_id || 0 }},
  u.system_code, u.username, u.alias_email, u.password_text, u.cat_profile, 1, u.wtu_cred_id
FROM work_team_un_credentials u
WHERE u.wtu_cred_id = {{ i_uncred_pick.selectedOptionValue }}
  AND u.is_assigned = 0;
