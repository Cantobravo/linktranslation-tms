SELECT 
  r.resource_id   AS value,
  CONCAT(r.resource_full_name, ' — ', COALESCE(r.resource_email, '')) AS label
FROM resources r
WHERE COALESCE(r.resource_is_active, 1) = 1
ORDER BY r.resource_full_name;
