UPDATE work_team_members
SET
  is_available = {{ j_avail_inline2.formData.is_available || NULL }},
  updated_at = NOW()
WHERE work_team_member_id = {{
  appsmith.store.availObj?.work_team_member_id
  || appsmith.store.wtmPrevRow?.work_team_member_id
  || tbl_work_team_preview.selectedRow?.work_team_member_id
  || 0
}}
LIMIT 1