SELECT
  template_id   AS value,
  CONCAT(template_name, ' (', template_type, ')') AS label
FROM templates
WHERE template_is_active = 1
  AND template_type IN ('Projects','HR')
ORDER BY template_name;
