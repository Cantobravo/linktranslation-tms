SELECT
  t.template_id AS value,
  t.template_name AS label,
  t.to_field,
  t.cc_field,
  t.bcc_field,
  t.subject_field,
  t.body_field
FROM templates t
WHERE LOWER(t.template_type) = 'resources'
  AND t.is_active = 1
ORDER BY t.template_name;
