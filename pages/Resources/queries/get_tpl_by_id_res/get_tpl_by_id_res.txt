SELECT
  t.template_id,
  t.template_name,
  t.to_field,
  t.cc_field,
  t.bcc_field,
  t.subject_field,
  t.body_field
FROM templates t
WHERE t.template_id = COALESCE({{ i_email_template_res.selectedOptionValue }}, NULL)
LIMIT 1;
