SELECT
  COUNT(1) AS cnt
FROM customer_contacts cc
WHERE
  LOWER(cc.customer_contact_email) = LOWER(COALESCE({{this.params.email}}, ''))
  AND COALESCE(cc.customer_contact_email, '') <> ''
  AND cc.customer_contact_id <> COALESCE({{this.params.exclude_id}}, -1);
