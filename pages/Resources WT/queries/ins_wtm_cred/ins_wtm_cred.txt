INSERT INTO work_team_member_credentials (
  work_team_member_id,
  username,
  alias_email,
  password_text,
  is_active,
  created_at,
  updated_at
)
VALUES (
  {{ j_cred_add.formData.work_team_member_id || null }},
  {{ j_cred_add.formData.username || null }},
  {{ j_cred_add.formData.alias_email || null }},
  {{ j_cred_add.formData.password_text || null }},
  1,
  NOW(),
  NOW()
);
