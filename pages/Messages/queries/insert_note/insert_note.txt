INSERT INTO linktranslation.notes
  (note_date, note_text, priority, status, created_by_name, created_by_email)
VALUES
  (
    {{ n_note_date.selectedDate ? moment(n_note_date.selectedDate).format('YYYY-MM-DD') : null }},
    {{ TextUtils.toPlain(n_note_text.text) }},      -- <- plain text now
    {{ n_priority.selectedOptionValue }},
    'Pending',
    {{ n_created_by_name.text }},
    {{ n_created_by_email.text }}
  );
