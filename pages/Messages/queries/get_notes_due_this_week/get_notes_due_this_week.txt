SELECT
  n.note_id,
  n.note_date,
  n.note_text,
  n.priority,
  n.status,
  n.created_by_name
FROM linktranslation.notes n
WHERE n.status = 'Pending'
  AND YEARWEEK(n.note_date, 1) = YEARWEEK(CURDATE(), 1)
ORDER BY n.note_date ASC, n.priority DESC, n.created_at DESC;
