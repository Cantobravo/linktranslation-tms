UPDATE linktranslation.notes
SET
  status = 'Completed'
  {{ /* Optional columns — only if you added them:
      , completed_by_name  = {{ appsmith.user.name || appsmith.user.username || appsmith.user.email }}
      , completed_by_email = {{ appsmith.user.email }}
      , completed_at       = CURRENT_TIMESTAMP
  */ "" }}
WHERE note_id = {{ notes_table.selectedRow.note_id }};
