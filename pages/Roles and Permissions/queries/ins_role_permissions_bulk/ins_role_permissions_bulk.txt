INSERT INTO role_permissions (role_id, permission_id)
VALUES {{
  (()=>{
    const roleId = Number(appsmith.store.roleRow?.role_id);
    const list = (ms_role_perms.selectedOptionValues || []).map(pid => `(${roleId}, ${Number(pid)})`);
    return list.join(',');
  })()
}};
