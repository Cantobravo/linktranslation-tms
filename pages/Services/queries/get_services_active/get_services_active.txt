SELECT
  s.service_id,
  s.service_name,
  s.service_status,
  s.created_at,
  s.updated_at
FROM services s
WHERE COALESCE(s.service_status, 'Active') = 'Active'
ORDER BY s.updated_at DESC, s.service_name;
